spring:
  application:
    name: springboot-grpc-sample

grpc:
  server:
    port: 9090
    reflection:
      enabled: true

  client:
    channels:
      local:
        address: static://localhost:9090